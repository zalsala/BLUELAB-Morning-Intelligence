(()=>{
const SECTION='안경 · 콘택트렌즈 · 안과 · 검안 · 시과학 · 근시관리';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const topic=s=>{const t=(s.title+' '+(s.summary||'')).toLowerCase();if(/사시|양안|vergence|binocular|stereo|복시|융합|폭주|조절/.test(t))return'양안시 · 사시 · 조절/폭주';if(/근시|myopia|안축|d i m s|dims|hal|stellest|아트로핀|orthokerat|ok렌즈|edof/.test(t))return'근시관리';if(/콘택트|contact|각막|cornea|kerato/.test(t))return'콘택트렌즈 · 각막';if(/망막|retina|녹내장|glaucoma|백내장|cataract|황반|macula/.test(t))return'안과질환';if(/시각|visual|optics|광학|contrast|acuity/.test(t))return'시과학 · 광학';return'검안 · 안과 임상';};
const kind=s=>{const e=(s.evidence_type||'').toLowerCase();if(e.includes('meta'))return'META-ANALYSIS';if(e.includes('random')||e.includes('rct'))return'RCT';if(e.includes('systematic'))return'SYSTEMATIC REVIEW';if(e.includes('review'))return'REVIEW';if(e.includes('guideline'))return'GUIDELINE';if(e.includes('regulator'))return'REGULATORY';if(e.includes('observ'))return'OBSERVATIONAL';return s.evidence_type||'RESEARCH / ISSUE';};
async function json(u){const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error(`${u}: ${r.status}`);return r.json();}
async function run(){const host=document.getElementById('visionResearchGrid'),count=document.getElementById('visionResearchCount');if(!host)return;try{const m=await json('./data/today.json');const files=m.story_files||[];const bundles=await Promise.all(files.map(f=>json('./data/'+f)));const items=bundles.flat().filter(s=>s.section===SECTION&&(s.evidence_type||/journal|ophthalm|optometr|vision|pubmed|arvo|jama|bmc/i.test((s.source||'')))).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))).slice(0,10);count.textContent=`현재판 ${items.length}개 · 근거수준 우선`;if(!items.length){host.innerHTML='<div class="researchEmpty">오늘 검증된 시과학·검안 논문 항목이 없습니다. 전문분야 검색창은 최대 30일까지 확대합니다.</div>';return;}host.innerHTML=items.map((s,i)=>`<article class="researchCard" tabindex="0" role="button" data-r="${i}"><div class="researchTop"><span class="researchKind">${esc(kind(s))}</span><span class="researchTopic">${esc(topic(s))}</span></div><h3>${esc(s.title)}</h3><p>${esc(s.summary||s.blue_summary||'')}</p><div class="researchMeta"><b>${esc(s.source||'')}</b><span>${esc(s.date||'')}</span><em>${esc(s.freshness_note||'')}</em></div></article>`).join('');host.querySelectorAll('.researchCard').forEach(el=>{const open=()=>{const s=items[Number(el.dataset.r)];if(typeof window.openStory==='function')window.openStory({...s,section:`VISION RESEARCH WATCH · ${topic(s)}`,points:[`근거수준: ${kind(s)}`,`분야: ${topic(s)}`,...(s.points||[])]});else window.open(s.url,'_blank','noopener');};el.addEventListener('click',open);el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}});});}catch(e){console.error(e);host.innerHTML='<div class="researchEmpty">연구 Watch 데이터를 불러오지 못했습니다.</div>';count.textContent='LOAD ERROR';}}
run();
})();
